name: journal
description: A new Flutter application.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  shared_preferences: ^0.5.8
  sqflite: ^1.3.1


flutter:

  uses-material-design: true

  assets:
    - lib/assets/create_table.txt
    - lib/assets/insert_query.txt
