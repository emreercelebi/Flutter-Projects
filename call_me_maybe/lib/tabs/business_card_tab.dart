import 'package:flutter/material.dart';

class BusinessCardTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('business car tab'),
    );
  }
}
