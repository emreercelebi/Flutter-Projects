import 'package:flutter/material.dart';

class PredictorTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Predictor tab'),
    );
  }
}